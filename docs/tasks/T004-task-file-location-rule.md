# T004 タスクファイル配置ルールの明記

## 背景

`docs/tasks.md` にタスクファイル名の規則はあるが、配置ディレクトリの明記がなかった。

## 対応

`docs/tasks.md` の「タスクIDのフォーマット」に次のルールを追加した。

- タスクファイルは `docs/tasks/` に配置する

## 結果

タスクファイルの保存先が文書化され、運用ルールが明確になった。
